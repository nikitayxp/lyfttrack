# LyftTrack App

App Expo (React Native) do LyftTrack — treinos, rotinas, stats e social.

```bash
npm install
npx expo start
```

Precisas de `app/.env` com `EXPO_PUBLIC_SUPABASE_URL` e `EXPO_PUBLIC_SUPABASE_ANON_KEY`.
